# Implementation Plans

Generated by the improve skill on 2026-08-15. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix format dispatch — support all 7 formats on Vercel/Netlify/Docker | P1 | S | — | DONE |
| 002  | Verification baseline — typecheck gate + lint cleanup | P1 | S | — | DONE |
| 003  | Translate UI/API/meta to English | P1 | M | — | DONE |
| 004  | Cross-target parity — DNS drift, fetch timeouts, QR privacy | P1 | M | 001 (QR flags) | DONE |
| 005  | Reproducible builds + client bundle — lockfile, pinned deps, flag icons | P2 | M | 002 (package.json) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 004 sets `supportsQR: true` for `wireguard`/`throne`/`wiresock` in
  `config/formats.ts` — the entries must already exist (from 001) and the QR
  leak must already be fixed in the same plan (004 rewrites `generateQR` in all
  three implementations before flipping the flags).
- 005 edits `package.json` after 002 added the `typecheck` script; keep it.
- 003 touches the same files as 004/001 in places (`config/formats.ts`,
  `lib/qr-generator.ts`, `worker/api-handler.js`, `functions/api/generate.js`).
  Run 001, 003, then 004 so translation happens before the parity rewrite.
  Each plan re-reads the file at start and matches on live content, so a
  one-step reorder is safe — but do not run 001 and 004 concurrently.

## Findings considered and rejected

- **Triplicated generation core (TD-01)**: the TS `lib/` builders, `worker/api-handler.js`
  and `functions/api/generate.js` each carry their own DNS/QUIC/builder copies.
  A full unification is a large refactor with real runtime risk across three
  deployment targets; deferred. The targeted parity fixes in 004 (DNS entries,
  timeouts, QR) reduce the drift surface now. Revisit if a third deployment
  target is added.
- **Re-enable formats tab UI (direction D1)**: `formats-tab.tsx` is dead and the
  tab is commented out in `topbar.tsx`. The format *support* is the real bug —
  covered by 001. The separate marketing tab is a product decision, not a bug.
- **commit `package-lock.json` vs pin exact versions**: pinned `buffer`/`tweetnacl`
  (005) gives reproducibility with a tiny diff; committing the existing lockfile
  (already in `.gitignore`) is the complementary half. Both are in 005.
- **CI workflows**: none exist in `.github/workflows/`; no job references
  `npm run lint`, so removing the deprecated `next lint` script (002) breaks
  nothing.

## Secrets noted (never print values)

- `README.md:298` and `README_ru.md:301` embed a star-history widget token.
  It is a third-party embed token, not a Cloudflare credential — flag for the
  repo owner to rotate/remove; out of scope for these plans (READMEs excluded).